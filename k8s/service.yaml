apiVersion: v1
kind: Service
metadata:
  name: url-shortener
  labels:
    app: url-shortener
spec:
  type: NodePort
  selector:
    app: url-shortener
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
